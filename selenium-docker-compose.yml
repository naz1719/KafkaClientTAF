# To execute this docker-compose yml file
# use `docker-compose -f selenium-docker-compose.yml up --build --abort-on-container-exit --scale chrome=3 --scale firefox=3`
version: "3"
services:
  chrome:
    image: selenium/node-chrome-debug
    volumes:
      - /dev/shm:/dev/shm
    depends_on:
      - hub
    environment:
      - HUB_HOST=hub
#    ports:
#      - "5555:5555"
#      - "5556:5900"
    # pwd to VNC Server : secret
    networks: ['selenium-grid']
  hub:
    image: selenium/hub
    ports:
      - "4444:4444"
    networks: ['selenium-grid']
  tests:
    image: automation_tests:1.0
    build:
      context: ./
      dockerfile: ./Dockerfile.dockerFile
    depends_on:
      - hub
    volumes:
      - ./.gradle:/root/.gradle
#    command: ["./wait-for-grid.sh", "{GRADLE_COMMAND}"]
    command: ["./wait-for-grid.sh", "./gradlew clean test --tests ui.SearchTextTest -Pbrowser=REMOTE"]
    networks: ['selenium-grid']
networks: {selenium-grid: {}}